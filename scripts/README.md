# scripts/ — Suite operational scripts

Status: `SCAFFOLD` — **no scripts exist yet.**

Scripts here operate on this repository only (e.g. link checking, manifest validation,
contract linting). Scripts must never mutate sibling repositories; cross-repo changes go
through the approval gates in `CLAUDE.md`.
