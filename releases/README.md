# releases/ — Suite release control

Status: `SCAFFOLD` — **no releases exist yet.** `manifests/` is intentionally empty.

`manifests/` will contain suite release manifests: for each suite release, the exact
version/commit of every product repository, the contract versions they satisfy, and the
compatibility statement. A manifest is created only for real, verified release candidates —
never speculatively.
